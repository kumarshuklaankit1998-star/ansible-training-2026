ansible-navigator:
  execution-environment:
    image: quay.io/ansible/creator-ee:v0.22.0
    pull:
      policy: missing

  ansible:
    config:
      path: ansible.cfg
    inventory:  # <--- This must be indented under 'ansible'
      entries:
        - lwinventory
